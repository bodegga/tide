# Tide Gateway Configuration

# DEPLOYMENT MODE
# - proxy: SOCKS5 + DNS only (no DHCP, no transparent routing)
# - router: DHCP + DNS + transparent proxy (default)
# - killawhale: Router + fail-closed firewall (blocks non-Tor)
# - takeover: Forced + ARP hijacking (advanced)
TIDE_MODE=router

# SECURITY PROFILE
# - standard: Default Tor settings (fastest)
# - hardened: Excludes 14-eyes countries
# - paranoid: Maximum isolation (slowest)
# - bridges: Use obfs4 bridges for anti-censorship
TIDE_SECURITY=standard

# NETWORK SETTINGS
TIDE_GATEWAY_IP=10.101.101.10
TIDE_SUBNET=10.101.101.0/24
TIDE_DHCP_START=10.101.101.100
TIDE_DHCP_END=10.101.101.200
